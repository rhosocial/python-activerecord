# requirements-3.8.txt
# Core data validation and serialization framework
# Pydantic provides model validation, serialization, and type conversion
# Version ~=2.10.6 is the latest compatible with Python 3.8
pydantic~=2.10.6

# Pydantic's core validation library written in Rust
# Provides high-performance validation and serialization
# Version ~=2.27.2 matches pydantic 2.10.6 requirements
pydantic-core~=2.27.2

# Testing framework for unit and integration tests
# Required for running the test suite
# Version ~=8.3.5 provides stable testing capabilities
pytest~=8.3.5

# Python packaging utilities
# Required for package building and distribution
# Version ~=75.3.2 is stable for Python 3.8
setuptools~=75.3.2

# Timezone info backport for Python 3.8
# Python 3.8 lacks native zoneinfo support, this provides compatibility
# Version ~=0.2.1 backports Python 3.9+ timezone functionality
backports.zoneinfo~=0.2.1