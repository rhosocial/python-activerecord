# dummy2

Second-generation DummyDialect offline suites: expression contracts and coverage gaps, statement builders (CREATE/DROP/ALTER/INSERT/UPDATE/DELETE/MERGE/EXPLAIN...), query sources (CTE/LATERAL/JSON TABLE/VALUES/set operations), mixins, data types, graph.
