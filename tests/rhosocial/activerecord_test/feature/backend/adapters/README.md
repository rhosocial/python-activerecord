# adapters

Value adapters for the SQLite backend: generic adapter registration/round-trip (`test_adapters.py`) and offline SQLite adapter round-trips for BLOB/JSON/UUID (`test_adapters_backend.py`).
