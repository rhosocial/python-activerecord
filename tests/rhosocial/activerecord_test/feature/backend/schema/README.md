# schema

Schema snapshot serialization and DataType serde (SQLite). Note: SQLite has no cross-schema support, so no `test_schema_support.py`; snapshot serialization tests serve as the schema subject.
