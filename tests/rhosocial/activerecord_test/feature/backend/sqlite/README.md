# sqlite

SQLite vendor-specific tests (common subjects were moved up to `feature/backend/*`):
- `extensions/` FTS5, R-Tree, GeoPoly, pragmas, extension integrations
- `examples/` example-derived tests (e.g. Sudoku solver)

Sync/async pairing status for the moved subjects is tracked in `.claude/testing.md` §5.2.
