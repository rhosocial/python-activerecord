# introspection

SQLite introspectors: tables/columns/indexes/FKs/views/triggers/database info, caches, to-SQL/expr-params, pragma/status introspectors, ALTER TABLE integration, 3.53+ features. Sync/async pairs co-located. `conftest.py` resets the version cache around each test.
