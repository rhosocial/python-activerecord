# query

Query feature tests: CTE, EXPLAIN (model/backend), expression round-trip/traversal, SQLite datetime-interval expressions, JSON protocol/path mode, EXPLAIN output modes, window protocol, named-expression procedures and procedure graphs. Sync/async gaps to be filled (Tier-2).
