Fix QUALIFY clause assembly in DQL statement formatting: remove the duplicated
`QUALIFY` keyword prefix and place the clause before `ORDER BY` (and after
`GROUP BY`/`HAVING`), matching Snowflake/BigQuery/ClickHouse clause ordering.