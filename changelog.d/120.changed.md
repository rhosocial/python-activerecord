Unify dialect formatting signatures to `(self, expr) -> Tuple[str, tuple]`, split the CREATE TABLE family into dedicated expressions with typed options and ALTER TABLE capability switches, and rename `JoinExpression`/`GroupingExpression` to `JoinClause`/`GroupingClause`.
