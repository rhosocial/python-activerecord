Fix `CREATE TABLE ... AS` rendering so the query is no longer parenthesised, and make unsupported DDL forms (column/constraint IF [NOT] EXISTS, CASCADE/RESTRICT, ADD CONSTRAINT) fail fast instead of emitting malformed SQL.
