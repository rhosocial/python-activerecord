Added batch execution interfaces `execute_batch_dml()` and `execute_batch_dql()` to the backend layer, supporting homogeneous DML expressions with RETURNING clause through expression-dialect system, and lazy pagination for DQL queries.
