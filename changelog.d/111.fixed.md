Fixed `format_add_index_action` in `DDLColumnMixin` to include the column list in generated SQL (e.g., `ADD INDEX "idx" ("col1", "col2")`). Previously only the index name was emitted, causing invalid ALTER TABLE statements for AddIndex alter actions.