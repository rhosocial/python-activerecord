Remove the duplicate ALTER TABLE actions `AddConstraint`/`DropConstraint`/`RenameColumn`, the `as_query`/`like_table` paths of `CreateTableExpression`, `supports_returning_clause()`, and dialect propagation.
