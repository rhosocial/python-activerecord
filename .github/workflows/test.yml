# .github/workflows/test.yml
name: Python Test and Coverage with TestSuite

on:
  push:
    branches:
      - main
      - 'release/v**'
      - 'maint/**'
  pull_request:
    branches:
      - main
      - 'release/v**'
      - 'maint/**'

permissions:
  contents: read
  pull-requests: write
  statuses: write

jobs:
  test-with-coverage:
    name: Run tests with Python 3.14 and coverage
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v3

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
        pip install codecov build

        pip install rhosocial-activerecord-testsuite==1.0.0.dev4

    - name: Run tests with coverage
      run: |
        export PYTHONPATH=src
        coverage run -m pytest tests/
        coverage xml
        coverage report

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ./coverage.xml
        flags: unittests,python-3.14
        name: codecov-umbrella-3.14
        fail_ci_if_error: false

  test-other-versions:
    name: Run tests with Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
    - name: Check out the repository
      uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        if [ "${{ matrix.python-version }}" == "3.8" ]; then
          pip install -r requirements-3.8.txt
          pip install -r requirements-dev-3.8.txt
        else
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
        fi

        pip install rhosocial-activerecord-testsuite==1.0.0.dev4

    - name: Run tests
      run: |
        export PYTHONPATH=src
        python -m pytest tests/

  test-free-threaded:
    name: Run tests with Python ${{ matrix.python-version }} free-threaded
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13t", "3.14t"]

    steps:
    - name: Check out the repository
      uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        if [ "${{ matrix.python-version }}" == "3.8" ]; then
          pip install -r requirements-3.8.txt
          pip install -r requirements-dev-3.8.txt
        else
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
        fi

        pip install rhosocial-activerecord-testsuite==1.0.0.dev4

    - name: Run tests
      run: |
        export PYTHONPATH=src
        python -m pytest tests/